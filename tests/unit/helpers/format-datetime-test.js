import { formatDatetime } from 'ask/helpers/format-datetime';
import { module, test } from 'qunit';

module('Unit | Helper | format datetime');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDatetime([42]);
  assert.ok(result);
});
